name: WildTTS / Finetuning
on:

  workflow_dispatch:
    

jobs:
  data-prep:
    runs-on: [self-hosted, arc]
    steps:
        - name: allocate_batch_job
          run: | 
            echo "Registered action"
            # cd ~/ensf619/WildTTS/training/campplus_finetuning